# Nexa — The Creative Operating System

> Create. Automate. Dominate.

## Group 1 — Foundation (complete)
- Landing page
- Auth (signup / login / email confirmation)
- Onboarding (workspace + brand setup)
- Dashboard shell (rail, topbar, AI chat)
- Full Supabase schema
- Nexa AI wired to Claude API

## Setup
See `DEPLOY.md` for complete step-by-step instructions.

```bash
npm install
cp .env.example .env.local
# fill in .env.local
npm run dev
```
