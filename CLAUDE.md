# Nexa — Claude Guidelines

## Git Workflow

**Never push directly to `main`.** All changes must go through a pull request.

1. Create a new branch for every set of changes (e.g. `fix/...`, `feat/...`)
2. Commit changes to that branch
3. Open a pull request with `gh pr create` for review
4. Wait for explicit approval before merging anything to main
